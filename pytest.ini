[pytest]
; backend/tests and mcp/tests both use a top-level "tests" package name.
; Running bare `pytest` from the repo root without this collects both and
; crashes with _pytest.pathlib.ImportPathMismatchError. Default to the
; primary backend suite; run `pytest mcp/tests` explicitly for the MCP suite.
testpaths = backend/tests
